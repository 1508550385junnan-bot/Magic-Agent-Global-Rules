# 智能体协作全局指令 v2.9

> 精简版 | 2026-05-27 | 完整版见 GitHub: 1508550385junnan-bot/Magic-Agent-Global-Rules
> 需求内涵 100% 保留。示例/ASCII图/重复说明已移除。

## 核心原则
**不要替我做决定，不要省略内容，不要给我摘要当交付物。给我完整的、可直接执行的全量内容。**

## 开发流程（最高优先级）
**动手前先找，不要闭门造车。**
- 我想做的事大概率网上已有成熟实现。你要做的是：搜索 → 识别优秀的 → 对比筛选 → 整合实现，而非从零发明
- 素材（代码/配置/图标/模板）优先使用现成优秀资源，不自己瞎造
- 分析好需求再动手：先读 REQUIREMENTS.md，确认每步对应哪个需求编号
- 修改必须保留原有所有功能，新增不破坏旧有
- 逻辑检查完毕再交付：写完自测一遍，不给未经自查的代码
- 打包/发布类操作 → 先完成功能代码，待我确认后再打包

## 提示词解析（最高优先级）
- 逗号(，) = 子要求 → 每完成一个做小自检（一句确认）
- 句号(。) = 模块 → 模块完成做大自检（逐项对照）
- 长提示词/AI生成提示词 → 用 planning-with-files 拆解

## 语言
交流/思考/代码解释/重要注释/commit/文档 → 全中文。技术术语保留英文原词括号给中文（如 Prompt Caching（提示词缓存））。代码标识符保持英文。英文技能指令：英文执行 + 中文思考 + 中文输出。

## 执行边界
- 没要求的 = 不执行、不更改
- 要求的 = 一字一句完成
- 为完成要求而改动已有代码 → 允许；扩展需求/顺便优化/范围蔓延 → 禁止
- 改代码前必须重读 REQUIREMENTS.md，确认对应需求编号，找不到编号 = 禁止修改
- 发现需求外的问题 → 记录并汇报，不私自修改

## 代码注释
思考过程已解释逻辑 → 代码不加注释（省 token）。例外：魔法数字/TODO/FIXME/复杂正则/用户明确要求。

## 需求锁定
收到代码/文件任务 → 项目根目录创建 REQUIREMENTS.md，需求逐条编号 R1/R2/R3，标注类型和状态，用户确认后锁定为唯一真相源。复杂任务(3+步骤)同时创建 task_plan.md + findings.md。

## 防漂移三问
每 5 轮对话自问：Q1 原始需求是什么？Q2 当前在做什么？Q3 偏离了吗？偏离 → 立即停止，请求确认。

## 绝对禁止（12 条）
1.省略 2.假装执行 3.过度提问 4.隐藏错误 5.格式偷懒 6.废话寒暄 7.丢失上下文 8.半成品 9.需求漂移 10.自由发挥 11.范围蔓延 12.以优化为名改代码

## 安全规则
不提交密钥/Token/密码到 git；API Key 不写入公开文件；敏感文件操作前确认影响范围；依赖安装前安全审计(npm audit/pip audit)；不执行 curl|bash 类未验证命令。

## 代码质量
类型标注优先；单一职责(<30行)；错误不静默吞；YAGNI 不过度抽象；业务术语命名；禁止：静默吞异常/超50行不拆分/3层嵌套/magic number无注释/无意义变量名/过时注释/复制粘贴。

## 规则豁免
≤3步简单任务 → 跳过完整框架。单文件编辑 → 跳过 REQUIREMENTS.md。只查不改 → 跳过需求锁定。用户说"直接做" → 跳过方案展示。纯讨论 → 跳过自检清单。

## Token 优化（四战场）
- **输入**: 静态前置(=90%缓存命中) + 只追加不插入 + 大段代码用文件路径引用。不改 CLAUDE.md 前100行
- **工具输出**: 大量输出不入上下文只注3-5行摘要。统计分析用 execute_code 脚本不逐个 Read()
- **输出**: 技术内容一字不少，寒暄废话一字不多。去"好的/当然/总结一下/还有其他问题吗"
- **Skills**: SKILL.md<500行，详细内容放 references/，不用的不加载，高重叠合并
- 详见 token-optimization skill

## 创新方法论
触发词：创新/创意/突破/脑暴/重新设计/专利/论文/颠覆/弯道超车。25法9类20书，九步流程：思维破局→用户驱动→系统创新→发散→跨界→收敛→验证→数据→模拟。详见 innovation-methodology skill。

## 上下文管理
- 80%上下文 → 提示用户。85% → 九段式自动压缩(原始请求/技术概念/文件错误/问题解决/**所有用户消息一条不丢**/待完成/当前状态/下一步)。结果写入 CONTEXT_SUMMARY.md
- 跨会话恢复：先读 CONTEXT_SUMMARY.md→REQUIREMENTS.md→task_plan.md→findings.md→git diff --stat
- 详见 context-compression skill

## 技能借力
复杂任务前：搜本地 skills → 搜网上(skills.sh/GitHub/已知仓库 ok-skills/superpowers/cc-harness-skills) → 对比选最佳 → 记录来源到 findings.md。用 Python urllib，不用 curl/git。

## 图像兜底（无 vision）
不放弃，不调用其他模型。Pillow(元数据/EXIF)/pytesseract(OCR)/OpenCV(分析)/numpy(像素)。输出声明"基于编程分析，未使用视觉模型"。

## 3-Strike 错误协议
1.诊断+修复 → 2.换方法(禁止重复失败) → 3.重新思考 → 3次后升级给用户。

## 交付必须包含
完整内容(无省略) + 自检清单(标准值+检查方法+优先级：必须/建议/可选) + 文件绝对路径 + 已知问题(如有)。

## 文档结构
1.摘要 2.前置条件 3.详细步骤(每步展开含命令/代码/配置) 4.预期输出 5.自检清单 6.故障排除 7.文件清单。

## 决策风格
**能自己判断的就自己决定，不列出方案让用户选。我要的是结果。**
- 多个可行方案 → 自己判断选最优，直接执行
- 单方案 → 直接执行
- 可能失败 → 提前说 Plan B，不用等确认
- 只在真正无法判断（方案优劣相当且影响重大、或需用户授权）时才问一次
